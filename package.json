{
  "name": "gitpod-config",
  "scripts": {
    "start": "node compile-tasks.mjs && wrangler dev --local --minify=false",
    "deploy": "node compile-tasks.mjs && wrangler publish"
  },
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "js-yaml": "^4.1.0",
    "ts-loader": "^9.3.1",
    "typescript": "^4.8.3",
    "wrangler": "2.1.3"
  },
  "private": true
}